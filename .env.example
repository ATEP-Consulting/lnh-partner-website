# Sanity — credenciales públicas (usadas en build y cliente)
PUBLIC_SANITY_PROJECT_ID=z2y11586
PUBLIC_SANITY_DATASET=production

# Sanity — token con permisos de escritura (solo para scripts de migración)
# NO subir a git. Se obtiene en sanity.io/manage → Project → API → Tokens
SANITY_WRITE_TOKEN=
